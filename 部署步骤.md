# 🚀 部署步骤清单

## ✅ 已完成

- [x] Git仓库已初始化
- [x] 代码已暂存（.env文件已排除）
- [x] 部署文件已准备完成

---

## 📋 接下来你需要完成的步骤

### 第1步：配置Git用户信息（如果你还没有）

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

或者只在当前项目配置：

```bash
cd time-healer-pro
git config user.name "你的名字"
git config user.email "你的邮箱"
```

### 第2步：创建Git提交

```bash
cd time-healer-pro
git commit -m "Initial commit: 时光治愈者项目"
```

### 第3步：创建GitHub仓库

1. 访问：https://github.com/new
2. Repository name: `time-healer-pro`
3. 选择 **Public** 或 **Private**
4. 点击 **Create repository**

### 第4步：连接GitHub仓库

**替换下面的 `你的GitHub用户名`**：

```bash
cd time-healer-pro

# 添加远程仓库（替换你的用户名）
git remote add origin https://github.com/你的GitHub用户名/time-healer-pro.git

# 推送代码
git branch -M main
git push -u origin main
```

### 第5步：部署到Vercel

1. 访问：https://vercel.com/new
2. 使用 **GitHub账号登录**
3. 选择 `time-healer-pro` 仓库
4. 点击 **Import**
5. 点击 **Deploy**（等待1-2分钟）

### 第6步：配置环境变量（重要！）

1. 部署完成后，点击 **Go to Dashboard**
2. 进入 **Settings** → **Environment Variables**
3. 添加新变量：
   - **Name**: `ZHIPU_API_KEY`
   - **Value**: `076d51eef15c496c844b27cdb23a7eeb.cPXG4U0Hu6bWMgaR`
4. 点击 **Save**

### 第7步：重新部署

1. 进入 **Deployments** 标签
2. 找到最新的部署
3. 点击 **⋯** → **Redeploy**
4. 等待重新部署完成

### 第8步：访问你的网站

部署成功后，点击 **Visit** 按钮或访问：

```
https://time-healer-pro.vercel.app
```

或

```
https://time-healer-pro-你的用户名.vercel.app
```

---

## 🔑 环境变量

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `ZHIPU_API_KEY` | `076d51eef15c496c844b27cdb23a7eeb.cPXG4U0Hu6bWMgaR` | 智谱AI密钥 |

---

## ⚠️ 重要提醒

### ✅ 安全性

- `.env` 文件已被 `.gitignore` 排除
- API密钥**不会**上传到GitHub
- 只在Vercel后台配置

### 📝 示例命令（复制使用）

**如果用户名是 `zhangsan`**：

```bash
# 配置Git
git config user.name "张三"
git config user.email "zhangsan@example.com"

# 创建提交
git commit -m "Initial commit: 时光治愈者项目"

# 连接GitHub
git remote add origin https://github.com/zhangsan/time-healer-pro.git

# 推送代码
git branch -M main
git push -u origin main
```

---

## 🐛 遇到问题？

### 问题：Git push失败

**解决**：
```bash
# 检查远程仓库
git remote -v

# 如果需要重新添加
git remote remove origin
git remote add origin https://github.com/你的用户名/time-healer-pro.git
```

### 问题：Vercel部署失败

**解决**：
1. 查看 Vercel Dashboard → Deployments → Logs
2. 检查是否有错误信息
3. 确保 `package.json` 脚本正确

### 问题：AI无法回复

**解决**：
1. 确认环境变量已配置
2. 重新部署项目
3. 查看Function Log

---

## 📞 需要帮助？

如果遇到问题，请告诉我：
1. 执行到哪一步出错了
2. 具体的错误信息
3. 截图（如果可能）

我会帮你解决！

---

**祝部署顺利！** 🚀